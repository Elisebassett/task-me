<div class="row">
	<h1>Welcome <%= current_user.username %></h1>
</div>
<div class="row">
	<ul>
		<% @tasks.each do |t| %>
			<input type="checkbox" id="test5" />
      <label for="test5">Red</label>



      
			<li><%= link_to "Complete", task_path(t, task:{complete: !t.complete?}), method: :patch, remote: :true %> <%= t.complete %> <%= t.title %> <%= link_to "Update", edit_task_path(t)%> <%= link_to "Delete", t, method: :delete, remote: :true %></li>
		<% end %>	
	</ul>
</div>